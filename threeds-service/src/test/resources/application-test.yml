spring:
  redis:
    host: localhost
    port: 6379

grpc:
  server:
    port: 0  # Random port for tests

threeds:
  acs:
    url: http://test-acs.example.com
  transaction:
    timeout-minutes: 10

logging:
  level:
    com.paymentgateway.threeds: DEBUG
